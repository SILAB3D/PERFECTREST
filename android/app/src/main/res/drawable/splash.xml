<?xml version="1.0" encoding="utf-8"?>
<!-- Ventana de arranque en Android 11 y anteriores (en 12+ manda
     `values-v31/styles.xml`). Vectorial: un solo fichero en vez de los diez
     PNG por densidad y orientación que traía la plantilla. -->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:drawable="@color/splash_background"/>
    <!-- 192dp y no 128: el drawable lleva dentro el margen que Android 12+
         necesita para su recorte circular, así que hay que pedirlo 1,5 veces
         más grande para que la marca salga del mismo tamaño que antes. Aquí
         no hay recorte, sólo se centra. -->
    <item
        android:gravity="center"
        android:width="192dp"
        android:height="192dp"
        android:drawable="@drawable/ic_splash_logo"/>
</layer-list>
